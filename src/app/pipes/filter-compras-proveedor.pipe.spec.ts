import { FilterComprasProveedorPipe } from './filter-compras-proveedor.pipe';

describe('FilterComprasProveedorPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterComprasProveedorPipe();
    expect(pipe).toBeTruthy();
  });
});
