import { FilterReportesProductosPipe } from './filter-reportes-productos.pipe';

describe('FilterReportesProductosPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterReportesProductosPipe();
    expect(pipe).toBeTruthy();
  });
});
